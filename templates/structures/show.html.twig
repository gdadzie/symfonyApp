{% extends 'base.html.twig' %}

{% block title %}Structures{% endblock %}

{% block body %}
    <h1>Structures</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ structure.id }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ structure.adresse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_structures_index') }}">back to list</a>

    <a href="{{ path('app_structures_edit', {'id': structure.id}) }}">edit</a>

    {{ include('structures/_delete_form.html.twig') }}
{% endblock %}
